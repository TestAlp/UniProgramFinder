const cheerio = require('cheerio');


var semesterAmountCollector = {}
htmlCollector.collect(html) = async function () {
    url = this.url;
    return allUrls;
};


//Get div with Id == ltlMufredat
function getDivFromId(html){
    var foundDive = '';
    return div;
}

function searchWithCheerio($) {
    $('.ProgramBaslikSatiri ').each(function (i, element) {
        getUrlAndAddToFoundElements(element);
    });
    distinct(urlsOfBachelorProgram);
}
//Get all Table with Class = tblMufredat
function getTableFromClass(div){
    var foundTable = '';
    addToArray
}

//Add to list
function addToArray(foundTables, foundTable){
    foundTabled.push(foundTable);
}
//Get count of list
function getAmountOfSemesters(string[] tables){
    return tables.length();
}

module.exports = htmlCollector;
